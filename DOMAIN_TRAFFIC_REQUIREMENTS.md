# 域名级流量明细需求与评估门

本文档定义“应用访问了哪些域名/主机、各自消耗多少流量”的后续路线。它是数据源和权限方案确定前的需求边界，不代表当前版本已经采集域名数据。

## 当前事实

ByteTrace 当前唯一的正式采集器是 `/usr/bin/nettop`：

```text
/usr/bin/nettop -n -P -d -x -L 0 -s 1
```

现有 CSV 解析链只消费以下字段：

| 字段 | 用途 |
| --- | --- |
| `time` | 样本时间 |
| 进程列（空列或 `process` / `process_name` / `name`） | 进程归属和应用归因 |
| `bytes_in` | 入站字节 |
| `bytes_out` | 出站字节 |

当前数据没有远端 IP、远端端口、DNS 名称、域名或 URL 字段。因此，不能从已有的应用级记录倒推出“某个应用访问了哪些网址”，也不能把 IP 反向猜成域名后作为真实流量明细展示。

## 产品定义

第一版只考虑“应用 → 域名/主机 → 流量”的尽力而为明细，不承诺完整 URL：

- 域名/主机是可展示的最小单位，优先展示稳定的 hostname；
- 无法关联的流量必须进入“未知域名/无法识别”，不能丢弃或静默归入某个域名；
- HTTPS 下的路径、查询参数和页面 URL 不属于通用第一版能力；
- 域名数据必须与应用级总量并列展示，不能替换当前稳定的 `nettop` 应用级统计；
- 该能力默认关闭，用户主动开启后才采集，并在设置页明确展示采集范围、权限和本地存储影响。

## 数据源评估

候选方案必须回答“这条连接由哪个应用产生、对应哪个远端主机、产生了多少字节、何时产生”四个问题。

| 方案 | 能解决的问题 | 主要缺口 | 结论 |
| --- | --- | --- | --- |
| 仅观察 DNS | 可获得部分域名与解析时间 | 不能证明域名实际消耗了多少字节，也可能遗漏缓存、DoH/DoT 和直连 IP | 只能作为辅助关联，不能作为流量主数据源 |
| 连接/流级观测 | 有机会获得远端主机、端口、连接生命周期和字节 | 需要验证应用归属、系统权限、网络变化和加密连接下的主机可见性 | 首选评估方向 |
| 代理或 TLS 检查 | 对特定流量可获得更丰富的主机或 URL 信息 | 权限、证书、隐私和兼容成本高，不能代表所有应用 | 不作为通用默认方案 |
| 浏览器或单应用扩展 | 可获得特定应用的页面级信息 | 覆盖范围窄，无法回答系统级应用流量 | 作为后续专项能力 |

在新增采集器前，必须先用独立的、可撤销的原型验证数据源，不直接把未经核对的结果接入正式 SQLite 或主窗口。

## 接入验收门

只有同时满足以下条件，才允许进入正式实现：

1. **归属可验证**：在进程启动、退出、PID 复用和网络切换场景下，连接仍能稳定关联到应用；无法关联的记录进入未知分类。
2. **流量可对账**：在同一时间窗口内，域名明细与 `nettop` 应用级总量能够对账，允许的差异有明确说明；不得因为域名采集重复计入应用总量。
3. **加密边界清楚**：只展示实际可观察到的 hostname；不把推断的域名、HTTPS 路径或查询参数当作事实。
4. **生命周期完整**：网络路径变化、睡眠唤醒、采集器异常退出和正常停止都能 flush 并重建基线，不遗留由应用创建的子进程。
5. **权限可解释**：安装和首次启用时能说明所需权限、采集范围和关闭方式；没有权限时应用级基线仍然可用。
6. **隐私可控**：默认关闭、仅本地存储、支持独立清理和保留周期；不保存请求内容、Cookie、凭据或完整 URL。
7. **性能可接受**：在持续运行和高连接数场景下，CPU、内存、数据库增长和采样丢失都有可量化上限。

## 第一阶段原型验收

正式开发前，先完成一个不修改正式数据的原型测试：

1. 使用 `curl`、浏览器和一个代理/后台进程构造已知目标流量；
2. 同时记录候选数据源的应用、远端主机、字节和时间，以及 `nettop` 的应用级统计；
3. 覆盖新建连接、连接复用、DNS 缓存、HTTPS、直连 IP、网络切换和睡眠唤醒；
4. 输出对账表：已知域名、未知流量、应用总量、差异和无法归属原因；
5. 只有对账和权限结果达标，才设计 `domain_usage` 数据表及主窗口 UI。

原型期间不修改 `daily_usage`、`usage_buckets` 和现有应用级统计口径，也不在 UI 中显示模拟域名数据。

## 暂不实现

- 从 IP 反查域名并当作确定事实；
- 对所有应用提供完整 URL 路径；
- 未经用户确认启用系统级网络拦截、代理或 TLS 检查；
- 在数据源、权限和对账方案未验证前增加域名列表、饼图或排名页面。

当前决策：继续以 `nettop` 应用级统计作为稳定基线，域名级采集停在原型评估门之前。
