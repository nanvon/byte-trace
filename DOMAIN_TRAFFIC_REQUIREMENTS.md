# 域名/主机流量排行需求与评估边界

状态：评估完成。当前产品允许增加基于连接级 `nettop` 的有限“可见网站/主机名排行”实验能力；这条路线不需要购买 Apple Developer Program 或支付 ¥688 年费。通用域名、完整 URL 和 HTTP 请求采集不纳入当前产品。本文保留数据源验证、失败原因和边界结论。

ByteTrace 的正式范围仍然是应用级流量统计；新增的可见网站/主机名排行是与应用级总量隔离的实验能力。系统级代理、TLS 检查和 Network Extension 运行化不再是开发任务。

## 免费前提下可以做什么

在只在自己的 Mac 上使用、明确不购买 Apple Developer Program 的前提下，允许开发以下本地实验能力：

- 继续使用系统自带的 `/usr/bin/nettop`，不要求 Network Extension entitlement；
- 使用不带 `-P` 的连接级输出，尽力取得远端 hostname、端口、协议和上下行字节；
- 做全局“可见网站/主机名 → 上传、下载、总量”排行；
- 在应用详情中做“该应用 → 可见网站/主机名”排行；
- 不在界面展示原始 IP，IP-only、hostname 缺失或无法归属的流量统一放入“无法识别/其他”；
- 展示可识别字节覆盖率，并明确标注“实验数据/部分可见”。

这条路线不需要每年 ¥688，因为它不申请 Network Extension 能力。Apple 中国大陆页面显示 ¥688 是 Apple Developer Program 的年度续订费用；Apple DTS 也明确说明免费 Personal Team 不支持 Network Extension Provider。因此，¥688 只与 Network Extension 的授权、签名、安装和启用有关，不是 `nettop` 粗略排行的前置条件。[Apple 中国大陆注册与续订说明](https://developer.apple.com/cn/help/account/membership/enrolling-in-the-app/)、[Apple 会员资格对比](https://developer.apple.com/cn/support/compare-memberships/)、[Apple DTS：免费 Personal Team 不支持 Network Extension Provider](https://developer.apple.com/forums/thread/128767)

`nettop` 是 socket/连接级观察工具，不是 HTTP 请求分析器；它可以提供部分连接目标和字节，但不能保证每条连接都有 hostname，也不能提供通用的完整 URL、路径、查询参数或请求内容。[nettop 手册](https://keith.github.io/xcode-man-pages/nettop.1.html)

## 当前事实

ByteTrace 当前唯一的正式采集器是 `/usr/bin/nettop`：

```text
/usr/bin/nettop -n -P -d -x -L 0 -s 1
```

现有 CSV 解析链只消费以下字段：

| 字段 | 用途 |
| --- | --- |
| `time` | 样本时间 |
| 进程列（空列或 `process` / `process_name` / `name`） | 进程归属和应用归因 |
| `bytes_in` | 入站字节 |
| `bytes_out` | 出站字节 |

当前数据没有远端 IP、远端端口、DNS 名称、域名或 URL 字段。因此，不能从已有的应用级记录倒推出“某个应用访问了哪些网址”，也不能把 IP 反向猜成域名后作为真实流量明细展示。

### 连接级原型发现

2026-08-02 在当前 macOS 上以不带 `-P` 的连接级模式读取了两帧真实 CSV：

- socket 行可以提供协议、本地/远端地址、接口、状态、`bytes_in` 和 `bytes_out`；
- 进程摘要行与 socket 行共用同一套表头，socket 行本身没有独立的进程列，需要依赖摘要行之后的输出分组；
- 使用 `-n` 时远端地址明确是 IP；去掉 `-n` 只是请求系统做名称解析，仍可能返回 IP，不能把 hostname 当成必然可用；
- 当前正式采集器仍使用 `-P`，连接级数据尚未进入应用级聚合、SQLite 或 UI。

因此，连接级数据可以支持一个明确降级的“应用 → 可见主机名 → 字节”排行原型，但它仍不等于完整域名流量：hostname 解析、代理转发、连接复用和未知流量必须单独标记，不得伪装成完整覆盖。

仓库提供 `ByteTraceConnectionProbe` 作为独立验证入口：

```bash
swift run ByteTraceConnectionProbe --duration 5
swift run ByteTraceConnectionProbe --duration 5 --numeric
swift run ByteTraceConnectionProbe --duration 5 --process codex
swift run ByteTraceConnectionProbe --duration 5 --runs 3 --process mihomo
```

探针使用不带 `-P` 的 `nettop` 连接级输出，只在内存中统计 hostname、IP、未知端点和进程摘要/连接明细的字节差异，不写入正式 SQLite、不修改应用级总量，也不显示在 UI。`--numeric` 用于关闭名称解析，`--process` 在内存中过滤进程，不把进程名直接传给 `nettop`，以避免短生命周期进程或 PID 变化导致整轮没有数据。

`--runs` 会顺序执行多个彼此独立的采样窗口，每轮重新建立首帧基线，最后输出四类状态的分布和 `reconciled` 百分比。它只用于稳定性验证，不把多轮结果合并进应用级流量统计。

### 2026-08-02 实机探针结果

在同一台 macOS 上分别运行 5 秒，两个结果是独立时间窗口，不能直接比较字节总量：

| 模式 | hostname 连接 | IP 连接 | 未知连接 | 进程/连接字节差异 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 默认名称解析 | 9 | 33 | 13 | 17.11% | `WARN` |
| `--numeric` | 0 | 43 | 9 | 25.97% | `WARN` |

默认模式的主要差异来自 `mihomo`、`Telegram` 和 `mDNSResponder`；numeric 对照中还出现过 1 条瞬时格式异常。探针本身能够完成 5 帧采集、首帧基线和端点分类，但连接明细与进程摘要没有达到当前设定的 5% 对账阈值。

当前决策：连接级 `nettop` 不能支撑精确的 `domain_usage` 或完整域名流量排名，但可以支撑独立的“可见网站/主机名排行”实验。首轮代理转发、UDP/mDNS、连接生命周期和进程分组专项对账已经完成；实验排行必须显示部分可见状态和覆盖率，不能改变应用级总量。

本轮受控流量基线（浏览器产生普通网页请求，持续 45 秒）：

- 完成 45 帧采集，1 帧基线，0 条 malformed row；
- hostname 连接 182 条、IP 连接 355 条、未知连接 47 条；这里的 hostname 只是 `nettop` 名称解析结果，不等于已验证的域名流量；
- 进程摘要与连接明细的绝对差异为 484,904 bytes，即 14.17%，仍为 `WARN`；
- 主要差异来自 `mihomo`（253,166 bytes）、`Dia`（142,116 bytes）和 `Telegram`（79,271 bytes）；其中部分进程有摘要流量但没有对应 socket 明细，代理转发和连接生命周期仍需专项拆解。

### 进程专项对账（内存过滤）

为避免把短生命周期进程、PID 变化或 `nettop -p` 的进程筛选行为误认为“没有连接数据”，探针现在先采集完整的连接级输出，再在内存中按进程名及其带 PID 的摘要名过滤。以下是 2026-08-02 的独立 20 秒样本；`PASS` 只表示本轮连接明细与进程摘要达到当前对账门槛，不代表已经具备通用域名采集资格。

| 进程 | 完成帧/基线帧 | hostname / IP / 未知连接 | 差异 | 结果 |
| --- | ---: | ---: | ---: | --- |
| `mihomo` | 20 / 1 | 34 / 119 / 0 | 4.81% | `PASS` |
| `mDNSResponder` | 20 / 1 | 0 / 0 / 6 | 0.00% | `PASS` |
| `Browser Helper` | 20 / 1 | 7 / 0 / 22 | 30.95% | `WARN` |
| `Telegram` | 20 / 1 | 0 / 56 / 0 | 49.24% | `WARN` |
| `Dia` | 20 / 1 | 0 / 0 / 0 | 100.00% | `WARN` |

专项结论：`mihomo` 和 `mDNSResponder` 在本轮可以对账，但 `Browser Helper` 有进程摘要没有对应的连接明细，`Telegram` 的连接明细只覆盖了部分进程流量，`Dia` 则出现摘要流量而没有端点明细。当前连接级数据仍不能作为所有应用的完整域名流量来源，继续停留在只读原型和专项诊断阶段。

探针现在将对账结果结构化输出为以下四类，进程级和全局级使用同一判定：

| 状态 | 含义 |
| --- | --- |
| `reconciled` | 进程摘要和连接明细都有流量，绝对差异不超过总摘要流量的 5%（小样本至少允许 1,024 bytes） |
| `partially_visible` | 两侧都有流量，但差异超过当前对账门槛，只能视为部分可见 |
| `summary_only` | 有进程摘要流量，但没有对应连接明细 |
| `unknown` | 没有进程摘要，或只有无法与进程摘要对上的连接流量 |

该分类只用于诊断和后续采集器评估，不改变现有应用级统计口径，也不代表 `hostname` 已经被验证为真实域名。

### 2026-08-02 多轮稳定性采样

使用 `--duration 5 --runs 3 --process <name>` 进行 3 轮独立采样，每轮重新建立基线。下表统计的是每一轮的全局对账状态；`unknown` 表示该轮没有匹配到进程流量，不能当作对账通过或失败。

| 进程 | 轮数 | `reconciled` | `partially_visible` | `summary_only` | `unknown` | 可对账比例 | 当前判断 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `mihomo` | 3 | 0 | 3 | 0 | 0 | 0.00% | 稳定地只能部分可见 |
| `Browser Helper` | 3 | 2 | 1 | 0 | 0 | 66.67% | 偶发摘要与连接明细不一致 |
| `Telegram` | 3 | 0 | 0 | 1 | 2 | 0.00% | 有效流量样本不足，需主动操作后复测 |
| `Dia` | 3 | 0 | 0 | 1 | 2 | 0.00% | 有流量时为仅摘要，需更多场景复测 |
| `mDNSResponder` | 3 | 2 | 0 | 0 | 1 | 66.67% | 活跃样本可对账，空闲轮不计入结论 |

Telegram 在用户主动发送消息/切换会话后，追加进行 3 轮、每轮 20 秒的有效流量复测：1 轮 `reconciled`、2 轮 `partially_visible`，可对账比例 33.33%；三轮端点均为 IP，没有 hostname。结果证明连接级明细可以看到部分 Telegram 流量，但不能稳定覆盖进程总量，也不能直接形成域名明细。

Dia 在用户主动打开/刷新网页后，追加进行 3 轮、每轮 20 秒的有效流量复测：3 轮均为 `summary_only`，进程摘要分别为 6,672、6,672 和 70,058 bytes，但连接明细始终为 0，未获得 hostname、IP 或未知端点。结果证明 Dia 的进程级流量在当前连接级 `nettop` 输出中不可见。

多轮结论：单轮 `PASS` 不能代表数据源稳定。当前没有一个目标应用满足“连续活跃样本都可对账”的条件；`mihomo` 的 3 轮都存在明显差异，`Browser Helper` 仍有间歇性摘要-only 情况，`Telegram` 在主动流量下仍以部分可见为主，`Dia` 即使在主动网页流量下也只有进程摘要。因此只允许做带覆盖率和“无法识别/其他”分类的实验排行，不承诺完整域名明细。

## 修正后的实验产品范围

第一版实验能力提供“可见网站/主机名 → 流量”的粗略排行，不承诺完整 URL 或请求级统计：

- 可展示的最小单位是连接级 hostname；界面不展示原始 IP；
- 无法关联 hostname、只有 IP、或者无法关联应用的流量必须进入“无法识别/其他”，不能丢弃或静默归入某个 hostname；
- 支持全局排行，也支持应用详情中的“该应用可见网站/主机名排行”；
- 结果必须保留上传、下载和总量，并标记样本时间范围、可见连接数和可识别字节覆盖率；
- 实验排行必须与正式应用级总量隔离，不能将连接级结果反向写回 `daily_usage` 或 `usage_buckets`；
- HTTPS 下的路径、查询参数和页面 URL 不属于通用第一版能力；
- 连接级数据存在应用差异、代理转发、连接复用和生命周期缺口，因此 UI 必须标注“实验数据/部分可见”；
- 该能力默认只在明确的实验页面显示，不替换当前稳定的应用级统计。

## 数据源评估

候选方案必须回答“这条连接由哪个应用产生、对应哪个远端主机、产生了多少字节、何时产生”四个问题。

当前候选数据源的权限、部署和历史原型记录见 [DOMAIN_TRAFFIC_SOURCE_DECISION.md](DOMAIN_TRAFFIC_SOURCE_DECISION.md)。以下方案表用于界定实验能力和明确不做的方案。

| 方案 | 能解决的问题 | 主要缺口 | 结论 |
| --- | --- | --- | --- |
| 仅观察 DNS | 可获得部分域名与解析时间 | 不能证明域名实际消耗了多少字节，也可能遗漏缓存、DoH/DoT 和直连 IP | 只能作为辅助关联，不能作为流量主数据源 |
| 连接/流级观测 | 有机会获得远端主机、端口、连接生命周期和字节 | 应用归属、网络变化、代理转发和加密连接下的主机可见性不稳定 | 允许做可见网站/主机名排行实验 |
| 代理或 TLS 检查 | 对特定流量可获得更丰富的主机或 URL 信息 | 权限、证书、隐私和兼容成本高，不能代表所有应用 | 不作为通用默认方案 |
| 浏览器或单应用扩展 | 可获得特定应用的页面级信息 | 覆盖范围窄，无法回答系统级应用流量 | 不进入当前产品 |

在新增采集器前，必须先用独立的、可撤销的原型验证数据源，不直接把未经核对的结果接入正式 SQLite 或主窗口。

## 可见网站/主机名排行实验验收门

实验排行进入独立数据模型和 UI 前，必须满足以下边界：

1. **不展示 IP**：hostname 可见时展示 hostname；IP-only、hostname 缺失和未知连接统一进入“无法识别/其他”。
2. **应用内排行**：只有能够关联到应用的连接才进入该应用的 hostname 排行，无法关联的流量不能静默归入其他应用。
3. **覆盖率透明**：展示可识别字节 / 应用级总量的覆盖率；覆盖率不足时明确显示“部分可见”。
4. **口径隔离**：连接级排行不修改、不重复累计正式 `daily_usage`、`usage_buckets` 和应用总量。
5. **时间一致**：使用和应用级排行相同的时间范围与本地时区口径。
6. **事实边界**：只展示实际观察到的 hostname；不从 IP 反查域名，不把完整 URL、路径、查询参数或请求内容当作事实。
7. **生命周期可控**：网络切换、睡眠唤醒、采集器异常退出和正常停止不会污染下一轮排行；每轮重新建立基线。
8. **本地隐私**：仅本地存储，支持独立清理；不保存请求内容、Cookie、凭据或完整 URL。

## 第一阶段原型验收（已结束）

此前的只读原型测试已经完成。以下步骤作为历史记录保留，不再作为正式开发前置任务：

1. 使用 `curl`、浏览器和一个代理/后台进程构造已知目标流量；
2. 同时记录候选数据源的应用、远端主机、字节和时间，以及 `nettop` 的应用级统计；
3. 覆盖新建连接、连接复用、DNS 缓存、HTTPS、直连 IP、网络切换和睡眠唤醒；
4. 输出对账表：已知域名、未知流量、应用总量、差异和无法归属原因；
5. 原计划只有对账和权限结果达标，才设计 `domain_usage` 数据表及主窗口 UI；当前改为设计独立的可见 hostname 实验数据模型，不使用 `domain_usage` 作为完整域名统计承诺。

原型期间不修改 `daily_usage`、`usage_buckets` 和现有应用级统计口径，也不在 UI 中显示模拟域名数据。

## 当前明确不实现

- 从 IP 反查域名并当作确定事实；
- 对所有应用提供完整域名、完整 URL 路径、查询参数、HTTP 请求或请求内容；
- Network Extension 系统扩展的签名、安装、用户授权和启用；
- 系统级透明代理、TLS 检查、限速代理或其他需要改变网络路径的功能；
- 将 DNS、浏览器扩展或用户手动代理结果作为当前排行的主数据源；
- 在不显示覆盖率和“无法识别/其他”的情况下增加 hostname 排名页面。

最终决策：继续以 `nettop` 应用级统计作为正式基线，并增加一个与正式统计隔离的连接级 hostname 实验排行。该排行支持全局和应用内两种视图，不显示 IP，不承诺完整覆盖；完整 URL、HTTP 请求和 Network Extension 方案不进入当前产品路线。
